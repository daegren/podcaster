<div class="content">
  <%= link_to "New Podcast", new_podcast_path, :class => 'btn' %>
  <% @podcasts.each do |p| %>
    <div class="podcast">
      <img src="<%= p.image %>" title="<%= p.title %>">
      <span class="title">
        <%= p.title %>
      </span>
    </div>
  <% end %>
</div>
